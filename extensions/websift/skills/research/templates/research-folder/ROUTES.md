# <Objective title> — route memory

| Route / candidate / hypothesis | Status | Evidence learned, reason, next action, or revisit condition |
|---|---|---|
| [...] | active | [...] |

Use: `active`, `accepted`, `rejected`, `deferred`, `near-miss`, `needs-trial`, or `superseded`.

A rejected row should preserve enough reason to prevent another agent from repeating the same attractive dead end. A near-miss must state the decisive requirement it fails.
